{
  "name": "headsign",
  "version": "0.4.0",
  "description": "A tiny phase gate for coding agents. Ask it what's next; it runs deterministic checks and tells the loop where to go.",
  "license": "MIT",
  "keywords": [
    "claude-code",
    "coding-agent",
    "phase-gate",
    "workflow",
    "state-machine",
    "cli",
    "loop-engineering",
    "graph-engineering"
  ],
  "type": "module",
  "bin": {
    "headsign": "plugin/dist/headsign.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meganemura/headsign.git"
  },
  "files": [
    "plugin/",
    "README.md",
    "README.ja.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --target=node20 --format=esm --outfile=plugin/dist/headsign.mjs --define:HEADSIGN_VERSION=\"\\\"${npm_package_version:?the version comes from npm; run the build with npm run build}\\\"\" --banner:js='#!/usr/bin/env node\nimport { createRequire } from \"node:module\";\nconst require = createRequire(import.meta.url);'",
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "esbuild": "^0.28.1",
    "@types/node": "^26.1.1"
  }
}
