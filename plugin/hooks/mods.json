{
  "description": "The function-hooks half of the plugin, one module: /headsign and the run pane it draws beside the transcript, refreshed after headsign shell commands and finished turns; and the actor stamp that names the session and agent in front of each headsign shell command. Loads only where Claude Code enables function hooks; the stop hooks stay in hooks.json.",
  "modules": ["./mod.ts"]
}
